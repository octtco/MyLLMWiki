# methods

这里放方法页。

方法页是正式页面类型，但不是所有顺手判断都要立刻建页。

适合升成方法页的情况：

- 它是可反复调用的判断或动作顺序
- 它有明确的触发时机或卡点
- 它能被多个主题复用

不适合急着建方法页的情况：

- 只是态度句
- 只是步骤清单
- 更像概念定义或主题综述

当前默认做法：

- 先在 `outputs/` 的候选池里收方法
- 确认稳定后，再升成 `wiki/methods/` 里的正式页面
