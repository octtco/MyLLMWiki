---
date: 2026-04-22
type: tool
status: seed
source: https://grok.868986.xyz/
tags:
  - seeds
  - tool
  - grok
  - mirror
---

# Grok 镜像站

## 看到的东西

- 用户提供了一个 Grok 的镜像站入口：<https://grok.868986.xyz/>
- 当前抓到的页面更像一个凭 `UserToken` 进入的访问页，而不是一个可直接保存正文的正式来源页。
- 页面上能明确看到的是输入框、`UserToken` 字样，以及“6-18 位字符即可免费使用”这类入口说明。

## 为什么先放这里

- 这条信息当前更像“可用入口线索”，不是稳定来源，也不是适合直接沉淀成正式知识页的对象。
- 它的可访问性、寿命、实际可用性和风险边界都很高波动，直接升进 `raw/` 反而会把入口线索误写成稳定来源。
- 所以这轮先继续留在 `seeds/`，只把它的性质写清楚：它是访问入口，不是知识来源。

## 后续可能去向

- outputs：如果某次任务实际用了这个入口，可以在对应输出里回链
- wiki：如果后续积累出一组更稳定的镜像站 / 替代入口，再考虑整理成正式页面
- raw：只有当后续真的通过它保存了明确来源内容时，才把“内容本身”迁进 `raw/`
