# todo

这里是独立于知识库的待办区。

- `tasks/`：一条待办一个 Markdown 文件
- `待办事项.base`：Obsidian Base 视图，按 `date` 分组看待办
- `_templates/`：新建待办时可复用的模板

建议做法：

1. 在 `todo/tasks/` 里新建一条待办
2. 按模板补 `date`、`status`、`priority`
3. 在 Obsidian 里打开 `todo/待办事项.base` 看总表
